# Environment for the standalone Design service (fractera-design, :3004) — step 197.
# Empty-but-real: architect-gated, no content yet. bootstrap.sh writes .env.local from these.

# --- Auth (browser redirects + server session read) ---
AUTH_SERVICE_URL=http://localhost:3001
# FRACTERA_IP_NODOMAIN_MODE=true opens the zone in IP/onboarding mode (auth bypass).

# When the design layer grows real content/data, add the same DATA/RAG/ADMIN keys the
# Projects service carries (see projects-app/.env.example).
