Send the project to the repository you just connected. Until this happens, GitHub
holds an empty shell and your code is still in one place only.

Press **Send project**, wait for it to finish, then press **Check project**. The
panel asks GitHub directly whether branch `main` carries a commit — not whether the
button was pressed. A push can fail quietly; an answer from GitHub cannot.
