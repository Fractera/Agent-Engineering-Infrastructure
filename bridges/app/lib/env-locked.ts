// Ключи окружения, которые панель менять НЕ ДАЁТ (шаг 501).
//
// Единственный список на два потребителя: маршрут `api/config/env` его применяет,
// страница `[lang]/env` по нему помечает поля. Прежде он существовал в двух
// копиях — в маршруте и в панели, — а расхождение двух копий означало бы, что
// страница обещает правку, которую сервер молча отвергнет.
//
// Почему эти ключи заперты: их значения выводятся из режима работы (домен против
// адреса), из набора языков или из устройства авторизации. Правка руками рассогласует
// их с тем, что настроено в других местах, и сломается не эта страница, а вход.
export const LOCKED_ENV_KEYS = new Set([
  "DATABASE_URL",
  "COOKIE_DOMAIN",
  "AUTH_TRUST_HOST",
  "NEXTAUTH_URL",
  "NEXT_PUBLIC_ADMIN_URL",
  "NEXT_PUBLIC_AUTH_URL",
  "ALLOWED_ORIGINS",
  "NEXT_PUBLIC_SUPPORTED_LANGUAGES",
  "NEXT_PUBLIC_DEFAULT_LOCALE",
]);
