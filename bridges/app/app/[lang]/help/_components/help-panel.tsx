// Разметка раздела «Справка» (владелец 2026-08-22).
//
// 🔒 СТРАНИЦА НАМЕРЕННО КОРОТКАЯ, И ЭТО РЕШЕНИЕ, А НЕ НЕДОДЕЛКА. Здесь стоял
// скелетон, а рядом жила целая группа «Документы» — двадцать три страницы, где
// панель показывала и правила документы проекта. Группа снесена: панель
// документами не управляет.
//
// Причина простая: у владельца уже есть тот, кто знает архитектуру целиком, —
// его собственный агент разработки. Он читает инструкции проекта на старте
// каждой сессии, видит код и конфиги, и отвечает на вопрос точнее любой
// страницы справки, потому что отвечает про ЭТОТ проект, а не про платформу
// вообще. Справочник, дублирующий его, устаревает первым же развёртыванием.
//
// Серверный компонент — в браузер не уезжает ни байта JS.

export function HelpPanel({ body }: { body: string }) {
  return (
    <div className="rounded-lg border border-border bg-card p-5">
      <p className="text-sm leading-relaxed text-muted-foreground">{body}</p>
    </div>
  );
}
