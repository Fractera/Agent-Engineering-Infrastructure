// Какие части резервной копии при загрузке ЗАМЕНЯЮТ то, что есть, а какие
// добавляют (шаг 501).
//
// ПОЧЕМУ ОТДЕЛЬНЫЙ ФАЙЛ, а не строка в `lib/backup-parts.ts`. Сначала я положил
// этот флаг туда — к списку частей, что выглядело логично. Сборка упала:
// `backup-parts.ts` импортирует `fs`, чтобы измерять размеры, а читает флаг
// КЛИЕНТСКИЙ островок загрузки — весь модуль вместе с `fs` поехал в браузер и не
// собрался. Правило простое: файл, который читает клиент, не смеет тянуть за
// собой серверные модули.
//
// Здесь только факт и ни одной зависимости, поэтому его безопасно читать с любой
// стороны. Живёт в папке раздела, а не в общей: потребитель ровно один. Появится
// второй — поднимется на один уровень, не выше.
//
// Это МАШИННЫЙ факт о поведении, а не слово: в файле переводов его могла бы
// «перевести» внешняя модель, и оранжевое предупреждение разошлось бы с
// реальностью. Машинная приёмка языков поймала именно это и была права.
export const REPLACES_ON_IMPORT = new Set(["knowledge", "config", "channels", "env"]);
