// Контракт данных раздела «parallel-routing» (шаг 501, каркас).
//
// Здесь описывается ровно то, что раздел читает и пишет, — и ничего сверх. Пока
// логика не переехала, тип пуст намеренно: пустой честный контракт лучше
// выдуманных полей, под которые потом придётся подгонять реальность.

export type ParallelRoutingState = Record<string, never>;
