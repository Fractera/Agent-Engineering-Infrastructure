// Frozen automation skeleton — VERSION 1 (the "coming soon" placeholder).
// The standard header and footer are provided by the Projects-zone layout (step 213),
// so this body is deliberately minimal: a centered title + "coming soon" line. Each
// development version of the frozen automation adds the next node/component here.
export default function AutomationEntry() {
  return (
    <main className="mx-auto flex min-h-[60vh] max-w-5xl flex-col items-center justify-center gap-2 px-4 text-center">
      <p className="text-2xl font-semibold">{{PROJECT_TITLE}}</p>
      <p className="text-sm text-muted-foreground">Project coming soon</p>
    </main>
  );
}
