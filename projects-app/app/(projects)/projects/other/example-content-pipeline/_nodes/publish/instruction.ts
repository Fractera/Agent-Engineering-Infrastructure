// The system instruction that generated this node's functions (co-located per node, step 223.C.2).
export const INSTRUCTION = `Turn a finished Article into a live, scheduled site page. Create the page,
schedule the publication for \`publishAt\`, and publish when due. All work is deterministic application
code — no AI. Type every function's inputs and return, and scope side effects to this node.`;
