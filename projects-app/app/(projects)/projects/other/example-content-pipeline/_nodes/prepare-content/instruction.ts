// The system instruction that generated this node's functions (co-located per node, step 223.C.2).
export const INSTRUCTION = `From the topic and the found sources, build a semantic outline, then draft
the article. Structuring is deterministic application code. Drafting genuinely needs generation, so it
is an EXPLICIT external AI tool-call step — never the application thinking for itself. Type every
function's inputs and return.`;
