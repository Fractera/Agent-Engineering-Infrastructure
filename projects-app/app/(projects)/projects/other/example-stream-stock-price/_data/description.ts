// Reference example (step 243) — identity.
export const PROJECT_DESCRIPTION = {
  title: "Example: Stock Price Lookup",
  description:
    "A reference STREAM automation: ask for a company's stock price by name, a real (non-AI) HTTP call resolves the current price, and a successful lookup is recorded — proving the stream ask console + a live dashboard table end to end.",
} as const;
