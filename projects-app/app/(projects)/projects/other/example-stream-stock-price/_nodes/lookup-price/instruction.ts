// The system instruction that generated this node's functions (co-located per node, step 243).
export const INSTRUCTION = `Build a deterministic function that fetches the CURRENT price for a given
ticker from a free, keyless quote service (no AI, no fabricated numbers). Throw a clear error when the
service does not return a usable live price — a step that found nothing must never report success. Every
function must be typed (inputs and return) and scoped to this node.`;
