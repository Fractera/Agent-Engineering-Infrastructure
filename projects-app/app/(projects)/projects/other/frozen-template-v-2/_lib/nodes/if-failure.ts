// ФУНКЦИЯ УЗЛА «CONDITION-FAILURE» — ветка отказа: тупик. По виду `condition-failure` выходного порта
// нет — из узла ничего не вытекает. Реальный отказ уже остановил цепочку броском в `transformPayload`
// (пустой захват), поэтому это честный именованный тупик: владелец ВИДИТ на холсте, где кончается
// провалившийся прогон. Имя `ifFailure` — публичный контракт.
//
// Контракт: (ctx) => частичный ctx. Ничего не добавляет и ничего дальше не передаёт (у узла нет выхода).
import type { NodeCtx } from "../executor";

export function ifFailure(_ctx: NodeCtx): Record<string, never> {
  return {};
}
