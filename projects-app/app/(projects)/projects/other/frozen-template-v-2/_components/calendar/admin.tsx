// КАЛЕНДАРЬ АДМИНИСТРАТИВНЫЙ — настройки вкладки: то, что дописывается ПОД публичной половиной
// и видно только владельцу. Посетителю не отдаётся никогда.
export default function CalendarAdmin() {
  return <section data-calendar="admin" />;
}
