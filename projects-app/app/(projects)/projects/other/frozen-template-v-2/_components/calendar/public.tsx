// КАЛЕНДАРЬ ПУБЛИЧНЫЙ — сама вкладка: сетка месяца и записи дня. Только чтение.
// Виден на обеих поверхностях: посетителю — как вся вкладка, владельцу — как её верхняя половина.
export default function CalendarPublic() {
  return <section data-calendar="public" />;
}
