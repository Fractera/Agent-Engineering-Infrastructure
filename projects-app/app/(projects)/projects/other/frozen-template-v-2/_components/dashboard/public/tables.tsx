import DashboardTablesClient from "./tables.client";

// ПУБЛИЧНАЯ ПОЛОВИНА дашборда — ВСЯ таблица целиком, в папке автоматизации (закон владельца 2026-07-24:
// логика таблицы живёт там, где её читает и правит агент по заявке «строить вместе с ИИ»).
//
// Здесь: универсальная таблица по объявлению из ядра (типы колонок, типизированные ячейки, действия строки,
// live-lookup), дебаунс-поиск, пагинация, выбор колонок с памятью браузера, карточка записи, разделённый вид
// «одна/две таблицы» и правка строк. Перенос первой версии один-в-один.
//
// `mode="view"` (витрина) — только чтение; `mode="admin"` (кокпит владельца) — с правкой строк.
export default function DashboardTables({ lang, mode }: { lang: string; mode: "view" | "admin" }) {
  return <DashboardTablesClient lang={lang} mode={mode} />;
}
