// The entity-section machinery (step 254.1) — the BASE-LAYER citizen ROUTE-V3 allows a route to import:
// the manifest shape (presence enum + resolver) and the one accordion frame. Entities declare, the
// platform renders.
export { EntitySection } from "./section";
export { resolvePresence, type EntityPresence, type EntitySectionManifest } from "./manifest";
