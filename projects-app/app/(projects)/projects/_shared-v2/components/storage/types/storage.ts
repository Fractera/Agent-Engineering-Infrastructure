// ТИПЫ микросервиса «склад» — ДЕВ/АДМИН-слой (`_shared-v2`). Кокпит-инструмент ручного добавления
// записи-изображения в объектное хранилище склада: живёт снаружи папки автоматизации, монтируется в неё
// через dev-slot, тянет crop из соседнего `_shared-v2/tools/image-crop`.
export type StorageAddObjectProps = {
  /** Имя таблицы базы данных склада, куда ложится строка со ссылкой на объект. */
  table?: string;
  /** Язык интерфейса (десять языков, англ. фолбэк — правило 4г). */
  lang: string;
};
