// ТИПЫ микросервиса «векторная память» — ДЕВ/АДМИН-слой (`_shared-v2`). Кокпит-инструмент ручного
// добавления записи в векторную память: имя + текст-факт + опционально изображение (crop → объектное
// хранилище → ссылка в `storageIds`). Живёт снаружи папки автоматизации, монтируется через dev-slot,
// crop берёт из `_shared-v2/tools/image-crop`. Третий склад v2 тем же образцом, что склад и локальная база.
export type VectorMemoryAddRecordProps = {
  /** Имя таблицы векторной памяти, куда ложится запись. */
  table?: string;
  /** Язык интерфейса (десять языков, англ. фолбэк — правило 4г). */
  lang: string;
};
