// ТИПЫ микросервиса «локальная база данных» — ДЕВ/АДМИН-слой (`_shared-v2`). Кокпит-инструмент ручного
// добавления строки: имя + опционально изображение (crop → объектное хранилище → ссылка в `storageIds`).
// Живёт снаружи папки автоматизации, монтируется через dev-slot, crop берёт из `_shared-v2/tools/image-crop`.
export type DatabaseAddRowProps = {
  /** Имя таблицы локальной базы, куда ложится строка. */
  table?: string;
  /** Язык интерфейса (десять языков, англ. фолбэк — правило 4г). */
  lang: string;
};
